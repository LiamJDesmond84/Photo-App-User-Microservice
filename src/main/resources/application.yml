server:
   port: 9001
   
spring:
   application:
      name: user-service
   devtools:
      restart:
         enabled: true
      
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
    instance:
      hostname: localhost
